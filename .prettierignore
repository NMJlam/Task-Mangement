node_modules
dist
frontend/dist
backend/drizzle
playwright-report
test-results
coverage
package-lock.json

# Vendored third-party agent skills — kept byte-identical to upstream
# (see skills-lock.json), so Prettier must not reflow them.
.agents/skills/

# Agent scratch space (git-ignored; Prettier does not read .gitignore)
.superpowers/
